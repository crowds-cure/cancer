{
  "name": "crowds-cure-cancer",
  "version": "1.0.0",
  "description": "TBD",
  "main": "index.js",
  "scripts": {
    "start": "gulp develop",
    "build": "gulp build && gulp sass && gulp copy:dist && gulp copy:html && gulp copy:imgs",
    "build-static-site": "npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "4.6.2"
  },
  "author": "crowds-cure",
  "repository": "https://github.com/crowds-cure/cancer",
  "license": "MIT",
  "dependencies": {
    "d3": "^5.7.0",
    "jquery": "^3.3.1",
  }
}
